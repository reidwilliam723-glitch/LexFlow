# Group Policy (enterprise — do not use for friend testing)

Testers: ignore this file. Start `Run\LexFlow.exe` instead.

IT admins can set `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\LexFlow` (and `\AI`, `\Privacy`, `\BlockedApps`). There is **no** `LexFlow-Setup.exe` in this test build.
